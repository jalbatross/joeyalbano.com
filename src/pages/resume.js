import React from 'react';
import resume from './resources-resume/resume.pdf';
const Resume = () => {
  return <iframe src={resume} style={{height:'11in',width:'8.5in',}}>f</iframe>
}

export default Resume;
